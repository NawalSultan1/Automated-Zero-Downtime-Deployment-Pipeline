---
- name: Deploying the application to live Server
  hosts: all
  become: true

  tasks:
    - name: Copy index.html to live Server
      copy:
        src: ../files/index.html
        dest: /var/www/html/index.html
        mode: '0644'
        owner: www-data
        group: www-data
